# Round 3 — independent audit of Questions 12 and 13

Three independent read-only passes checked the chapters against the official
annotations, the mapped local evidence, and authoritative operating-system and
networking specifications where the local notes were incomplete or inaccurate.

## Result

- No P0 defect was found.
- Question 12 covers every item in its official annotation. Its remaining
  high-level risk is provenance: the prescribed literature [21] and [46] is
  still absent from the corpus.
- Question 13 covers every required section. The earlier global-unicast defect
  was already corrected; this pass found missing algorithmic depth and several
  overbroad source-derived statements.

## Corrections applied

Question 12 now qualifies `SIGCHLD` reaping and `_exit`, names the process-group
and session APIs, and completes the System V semaphore and message-queue API
families.

Question 13 now includes the distance-vector Bellman--Ford update rule and
count-to-infinity mitigations, CIDR aggregation and longest-prefix matching,
accurate IPv6 anycast/mobility/address notation, exact TCP handshake sequence
acknowledgements, the registry/registrar distinction, and recursive-versus-
iterative DNS resolution with authoritative servers and TTL caching.

## Remaining status

Both chapters are exam-ready at the requested concise depth. Question 12 should
still be checked against its missing prescribed literature before a final
academic edition.
